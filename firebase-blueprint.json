{
  "entities": {
    "feedback": {
      "title": "Visitor Feedback",
      "description": "Feedback and traces left by visitors of the portfolio.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the visitor."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email of the visitor for replies."
        },
        "content": {
          "type": "string",
          "description": "The feedback or insights shared by the visitor."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the feedback was submitted."
        }
      },
      "required": ["name", "email", "content", "createdAt"]
    }
  },
  "firestore": {
    "/feedback/{feedbackId}": {
      "schema": "feedback",
      "description": "Collection of visitor feedback."
    }
  }
}
